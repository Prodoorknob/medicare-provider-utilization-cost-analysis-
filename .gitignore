# Raw data and partitioned outputs (large, generated at runtime)
data/
partitioned_data/
local_pipeline/

# web/public/data is intentionally tracked (synced investigation briefs)
!web/public/data/
!web/public/data/**

# Python artifacts
__pycache__/
*.py[cod]
*.pyo
.env
.venv/
env/
venv/

# Jupyter / IDE
.ipynb_checkpoints/
.vscode/
.idea/

# MLflow local runs
mlruns/
mlartifacts/

# Web app
web/node_modules/
web/.next/
web/.env.local

# NOTE: deploy/railway-pipeline/seed/ (43 MB static parquet) is intentionally NOT
# gitignored so `railway up` includes it in the build context. It is also not
# committed — leave it untracked and regenerate with deploy/railway-pipeline/make_seed.py.

# Falsification test bulk inputs (re-downloadable CMS RVU/PFS files; reports and scripts are tracked)
falsification_test/rvu_zips/
falsification_test/rvu_extracted/
falsification_test/rvu_page.html

# Claude Code session artifacts
.claude/scheduled_tasks.lock

# OS
.DS_Store
Thumbs.db
